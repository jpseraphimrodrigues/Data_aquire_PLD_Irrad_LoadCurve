[project]
name = "data-aquire-pld-irrad-loadcurve"
version = "0.1.0"
description = "Aquisição, tratamento e visualização de PLD, irradiância solar e curva de carga."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.8",
    "pandas>=2.0",
    "requests>=2.31",
    "xlwt>=1.3",
]

[dependency-groups]
dev = [
    "jupyter>=1.0",
]

[tool.uv]
package = false

